<section
  class="relative py-20 px-6 scroll-fade-in visible font-sans text-left flex items-center justify-center flex-col text-signal-white object-cover"
  style="background-image: url('{{ "site-banner-bg.png" | asset_url }}'); background-size: cover; background-position: center center; background-repeat: no-repeat;"
>
  <div class="absolute inset-0 bg-[rgba(0,0,0,0.8)] bg-blend-color-darken"><p></p></div>
  <div class="container mx-auto px-4  transition-all scroll-trigger animate--slide-in duration-1000 delay-200">
    <div class="max-w-7xl mx-auto text-center">
      <p class="text-[17px] text-white leading-relaxed mb-12">{{ section.settings.top_text }}</p>
      <div
        class="p-8 rounded-[8px] mb-12 bg-[#0d0d0d4f]"
      >
        <h2 class="text-[30px] font-bold text-[#afa38e] mb-6">{{ section.settings.video_title }}</h2>
        <div class="mb-6">
          <div
            class="relative w-full h-0 pb-[56.25%] rounded-[8px] overflow-hidden"
          >
            <iframe
              src="{{ section.settings.video_url }}"
              allowfullscreen=""
              allow="fullscreen"
              class="absolute top-0 left-0 w-full h-full"
            ></iframe>
          </div>
        </div>
        <p class="text-[#7b0f23] leading-relaxed text-3xl">{{ section.settings.bottom_text }}</p>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Video Card",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "top_text",
      "label": "Top Text",
      "default": "The eFit 3D Scanner represents a breakthrough in non-contact ear scanning technology, delivering the precision and safety that traditional methods simply cannot match. As the world's only FDA-cleared, non-contact 3D ear scanner, the eFit system transforms how custom hearing protection is created while establishing the foundation for advanced biometric capabilities."
    },
    {
      "type": "text",
      "id": "video_title",
      "label": "Video Title",
      "default": "Zero Contact. Zero Risk. Maximum Precision."
    },
    {
      "type": "text",
      "id": "video_url",
      "label": "Video Url",
      "default": "https://www.canva.com/design/DAGtO6bmHNg/1fMTN4pC8ZG6c2waB1oo8w/watch?embed"
    },
    {
      "type": "text",
      "id": "bottom_text",
      "label": "Bottom Text",
      "default": "Revolutionary Non-Contact Technology"
    }
  ],

  "blocks": [],
  "presets": [
    {
      "name": "Video Card",
      "category": "3D Scanner cards"
    }
  ]
}
{% endschema %}
